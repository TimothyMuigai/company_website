import type { Metadata } from "next";

/** Enterprise due-diligence SEO for the transparent workflow-assessment intake. */
export const metadata: Metadata = { title: "Workflow Exposure Assessment", description: "Outline the evidence, ownership, review, escalation, and decision-record requirements behind a consequential workflow.", alternates: { canonical: "/assessment" } };
export default function AssessmentLayout({ children }: { children: React.ReactNode }) { return children; }
