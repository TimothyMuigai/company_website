import type { Metadata } from "next";
/** SEO metadata for the enterprise assessment contact path. */
export const metadata: Metadata = { title: "Request a Due-Diligence Assessment", description: "Discuss an enterprise workflow involving identity, media, documents, or AI-generated evidence with Deeptrack.", alternates: { canonical: "/contact" } };
export default function ContactLayout({ children }: { children: React.ReactNode }) { return children; }
