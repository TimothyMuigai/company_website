"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import EnterpriseHeader from "@/components/enterprise/EnterpriseHeader";
import EnterpriseFooter from "@/components/enterprise/EnterpriseFooter";

/** Deeptrack enterprise due-diligence design: retained gated news resource, reframed as intelligence without unsupported update-frequency claims. */
export default function News() {
  const router = useRouter(); const [name, setName] = useState(""); const [institution, setInstitution] = useState(""); const [email, setEmail] = useState(""); const [loading, setLoading] = useState(false); const [success, setSuccess] = useState(""); const [error, setError] = useState("");
  const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); setError(""); setSuccess(""); setLoading(true); try { const response = await fetch("/api/news-centre", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, institution, email, message: "Due-Diligence Intelligence Access Request" }) }); const data = await response.json(); if (!response.ok) throw new Error(data?.message || "Submission failed. Try again."); setSuccess("Thank you. Your access request has been received."); setName(""); setInstitution(""); setEmail(""); setTimeout(() => router.push(`/news-centre-portal?token=${encodeURIComponent(data.accessToken || "")}`), 1200); } catch (requestError: unknown) { setError(requestError instanceof Error ? requestError.message : "Network error. Please try again."); } finally { setLoading(false); } };
  const input = "mt-2 w-full border-b border-[#99D3F0] bg-transparent py-3 text-sm outline-none placeholder:text-[#808080] focus:border-[#0191DA]";
  return <div className="min-h-screen bg-white text-[#333333]"><EnterpriseHeader /><main><section className="bg-[#F2F9FD]"><div className="mx-auto grid max-w-[1180px] gap-12 px-5 py-16 lg:grid-cols-[.85fr_1.15fr] lg:px-10 lg:py-24"><div><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-[#0191DA]">Due-diligence intelligence</p><h1 className="mt-5 font-[family-name:var(--font-space-grotesk)] text-5xl font-medium tracking-[-0.06em] sm:text-6xl">Evidence-risk context for the decisions that cannot wait.</h1><p className="mt-7 max-w-md text-base leading-7 text-[#808080]">The News Center is a gated intelligence resource for journalists, researchers, and practitioners monitoring digital-evidence and synthetic-media developments.</p><Link href="/research" className="mt-7 inline-flex text-sm font-semibold text-[#0191DA] hover:text-[#34A7E1]">Explore Deeptrack research →</Link></div><form onSubmit={submit} className="bg-white p-6 sm:p-9"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-[#0191DA]">Request access</p><label className="mt-6 block text-sm font-medium">Full name<input value={name} onChange={(event) => setName(event.target.value)} required placeholder="Your name" className={input} /></label><label className="mt-6 block text-sm font-medium">Institution or organisation<input value={institution} onChange={(event) => setInstitution(event.target.value)} placeholder="Organisation name" className={input} /></label><label className="mt-6 block text-sm font-medium">Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required placeholder="name@organisation.com" className={input} /></label>{success && <p className="mt-5 text-sm leading-6 text-[#0191DA]">{success}</p>}{error && <p className="mt-5 text-sm leading-6 text-[#333333]">{error}</p>}<p className="mt-6 text-xs leading-5 text-[#808080]">Read the <Link href="/privacy-policy" className="underline decoration-[#0191DA] underline-offset-4">Privacy Policy</Link> to understand how submitted information is handled.</p><button disabled={loading} className="mt-7 bg-[#0191DA] px-5 py-4 text-sm font-semibold text-white hover:bg-[#34A7E1] disabled:opacity-60">{loading ? "Submitting…" : "Request News Center access"}</button></form></div></section><section className="mx-auto max-w-[1180px] px-5 py-20 lg:px-10"><div className="grid gap-6 md:grid-cols-3">{[["Monitor", "Follow developments relevant to evidence, identity, media, and AI-enabled manipulation."], ["Investigate", "Use the resource as a starting point for research and reporting—not as a substitute for your own verification."], ["Contextualize", "Connect current developments to the operating decisions, controls, and review responsibilities they may affect."]].map(([title, body], index) => <article key={title} className="border-t border-[#CCE9F8] py-7"><span className="text-sm text-[#0191DA]">0{index + 1}</span><h2 className="mt-6 font-[family-name:var(--font-space-grotesk)] text-2xl font-medium">{title}</h2><p className="mt-3 text-sm leading-6 text-[#808080]">{body}</p></article>)}</div></section></main><EnterpriseFooter /></div>;
}
