import type { Metadata } from "next";
export const metadata: Metadata = { title: "Research Resource", description: "Read a Deeptrack research resource for evidence-led enterprise decision workflows." };
export default function ResearchResourceLayout({ children }: { children: React.ReactNode }) { return children; }
