"use client";

import EnterpriseHeader from "@/components/enterprise/EnterpriseHeader";

/** Deeptrack enterprise due-diligence design: legacy public routes reuse the complete responsive public navigation. */
export function Navbar() {
  return <EnterpriseHeader />;
}
